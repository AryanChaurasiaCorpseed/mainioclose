import React from 'react'

const LeadChilds = ({leadid}) => {
    const singleLeadResponseData = useSelector(
        (state) => state.leads.singleLeadResponseData
      )
  return (
    <div>LeadChilds</div>
  )
}

export default LeadChilds